{
    "name": "frontend",
    "version": "0.1.0",
    "description": "",

    "scripts": {
        "install-backend": "cd Backend && npm install",
        "install-frontend": "cd Frontend && npm install",
      "start-frontend": "cd Frontend && react-scripts start",
      "start-backend": "cd Backend && nodemon --watch src --exec ts-node src/index.ts",
      "build-backend": "cd Backend && tsc",
      "build-frontend": "cd Frontend && react-scripts build"
    },
    "eslintConfig": {
      "extends": [
        "react-app",
        "react-app/jest"
      ]
    },
    "browserslist": {
      "production": [
        ">0.2%",
        "not dead",
        "not op_mini all"
      ],
      "development": [
        "last 1 chrome version",
        "last 1 firefox version",
        "last 1 safari version"
      ]
    },
    "devDependencies": {
      "@types/jwt-decode": "^3.1.0",
      "@types/react-router-dom": "^5.3.3"
    }
  }
  